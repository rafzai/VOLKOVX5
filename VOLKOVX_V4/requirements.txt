# VOLKOVX BOT v4.1 — Dependencies

# ── Core async networking ────────────────────────────────────
aiohttp>=3.9.5,<4.0.0
websockets>=12.0                    # untuk legacy data_pipeline.py

# ── Config loader ────────────────────────────────────────────
python-dotenv>=1.0.1

# ── Terminal UI ──────────────────────────────────────────────
rich>=13.7.1

# ── Polymarket CLOB client (untuk generate_api_creds.py) ────
py-clob-client>=0.15.0

# ── Testing (untuk test_volkovx.py + test_core.py) ──────────
pytest>=7.4.0
pytest-asyncio>=0.21.0

# ── Optional performance boosts ──────────────────────────────
# uvloop>=0.19.0; sys_platform != "win32"   # 2-4x event-loop speedup
# orjson>=3.10.0                            # 3x faster JSON parse
